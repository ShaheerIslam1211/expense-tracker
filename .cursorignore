*node_modules# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*dist# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*dist-ssr# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
**.local# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*logs# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*npm-debug.log*# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*yarn-debug.log*# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*yarn-error.log*# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*pnpm-debug.log*# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*lerna-debug.log*# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*__pycache__# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
*__pycache__# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
